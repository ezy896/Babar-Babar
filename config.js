// Babar Ali Law Associates — Configuration & Environment

window.BALA_CONFIG = {
  // Replace these with your live Supabase credentials if desired
  supabaseUrl: 'https://xyzcompany.supabase.co',
  supabaseAnonKey: 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.dummy_key',
  // Fallback to offline localStorage simulation mode if credentials are dummy
  useLocalStorageFallback: true
};
